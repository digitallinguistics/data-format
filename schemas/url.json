{
  "$schema": "http://json-schema.org/schema#",
  "id": "http://cdn.digitallinguistics.io/schemas/url-1.0.1.json#",
  "title": "URL",
  "type": "string",
  "format": "uri",
  "description": "A URL where this resource can be retrieved. The resource does not have to be publicly available at this URL; it may require permissions to access. Best practice is to make the resource available in both HTML or JSON format at this URL, depending on the `Accepts` header of the request."
}
