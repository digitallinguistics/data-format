{
  "$schema": "http://json-schema.org/schema#",
  "id": "http://digitallinguistics.org/schemas/url-1.0.0.json#",
  "title": "URL",
  "type": "string",
  "format": "uri",
  "description": "A URL where this resource can be retrieved. The resource does not have to be publicly available at this URL; it may require permissions to access."
}
